

export const initModels = () => {

    // User and Product association through chat_x_chat_member
    // User.belongsToMany(Product, { through: UserXProduct, foreignKey: "id_user" });
    // Product.belongsToMany(User,{through:UserXProduct,foreignKey:"id_product"})
  
    
  };
  